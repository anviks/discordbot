[project]
name = "max-bot"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord-py>=2.7.1",
    "fastapi>=0.135.3",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.44.0",
]

[project.scripts]
max-bot = "max_bot.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
